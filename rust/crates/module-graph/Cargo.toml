[package]
name = "ignite-module-graph"
edition.workspace = true
version.workspace = true

[dependencies]
ignite-fs-utils = { path = "../fs-utils" }
regex = "1"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
