[package]
name = "ignite-auto-fix"
edition.workspace = true
version.workspace = true

[dependencies]
once_cell = "1"
regex = "1"
serde_json = { version = "1", features = ["preserve_order"] }

[dev-dependencies]
tempfile = "3"
