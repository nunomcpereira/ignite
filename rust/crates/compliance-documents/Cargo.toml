[package]
name = "ignite-compliance-documents"
edition.workspace = true
version.workspace = true

[dependencies]
ignite-fs-utils = { path = "../fs-utils" }
serde = { version = "1", features = ["derive"] }
regex = "1"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
