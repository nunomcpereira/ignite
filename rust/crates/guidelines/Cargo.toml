[package]
name = "ignite-guidelines"
edition.workspace = true
version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
